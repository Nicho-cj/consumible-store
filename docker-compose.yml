version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: db_ordenes_servicio_tecnico
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./init-db:/docker-entrypoint-initdb.d

  # Aquí definiremos el backend y frontend

volumes:
  postgres_data:
